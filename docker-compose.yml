services:
  app:
    image: ghcr.io/kalisam2015/new-canvas:latest
    container_name: infinite-canvas
    ports:
      - "3000:3000"
    restart: unless-stopped
    # GA4 统计（可选，默认关闭）：
    # environment:
    #   ANALYTICS_GA4_ID: G-XXXXXXXXXX   # Google Analytics 4 衡量 ID
